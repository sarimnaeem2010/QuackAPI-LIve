{
  "name": "quackapi-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install": "cd QuackAPI && npm install",
    "dev": "cd QuackAPI && npm run dev",
    "build": "cd QuackAPI && npm run build",
    "start": "cd QuackAPI && npm run start",
    "auto-push": "node scripts/auto-push.js"
  },
  "dependencies": {
    "chokidar": "^4.0.3"
  }
}
